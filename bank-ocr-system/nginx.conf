server {
    proxy_read_timeout 600;
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    send_timeout 600;

     location /_next/static/ {
        alias /path/to/your/nextjs/app/.next/static/;  # Replace with actual path to the static folder
    }
}